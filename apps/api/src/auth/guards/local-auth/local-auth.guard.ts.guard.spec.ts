import { LocalAuthGuardTsGuard } from './local-auth.guard.ts.guard';

describe('LocalAuthGuardTsGuard', () => {
  it('should be defined', () => {
    expect(new LocalAuthGuardTsGuard()).toBeDefined();
  });
});
